# PostgreSQL (development & production)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://postgres:adipta2806@localhost:5432/pendataan_karyawan?schema=public"

# NextAuth.js v5 — generate dengan: npx auth secret
# atau: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_SECRET="ganti-dengan-string-acak-32-byte-hex"
